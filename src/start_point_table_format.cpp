#include "start_point_read_write.h"
void start_point_read_write::start_point_csv() {

  // std::thread o(&only_read_serial::only_read_serial_read, &reader);
  //  std::thread s(&log_file_serial::log_to_file_csv, &logger);
  // o.join();
}
