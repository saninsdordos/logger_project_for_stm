#include "open_serial_port.h"
#include <cstddef>
#include <cstdio>
#include <cstdlib>
bool state_search_type_port() {
  int t = 0;
  std::string messagess = "NUmber please";
  for (size_t s = 0; s < 4; s++) {

    //    case 'y': {
    //     int temp = port_usb[s + 1] - '0';
    //    if (temp >= 0) {
    //     open_usb_port();
    //  } else {
    //   exit_from_programm(messagess, 1);
    //}
  }
  return true;
}
